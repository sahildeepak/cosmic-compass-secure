# This file tells Netlify to increase the timeout and max size for your function

# Specific configuration for your generateReading function
# Configure both timeout and maximum payload size for image uploads
[functions."generateReading"]
  timeout = 26
  max_size = "10mb"